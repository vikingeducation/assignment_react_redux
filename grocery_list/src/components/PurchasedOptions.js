import React from "react";

const PuchasedOptions = ({ onChange }) => {
  <select onChange={onChange}>
    <option value="PURCHASED">Purchased</option>
    <option value="NOT_PURCHASED">Not Purchased</option>
    <option value="ALL">all</option>
  </select>;
};
